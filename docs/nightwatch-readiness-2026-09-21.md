# JUNKMAN / Nightwatch 检查：2026-09-21

检查时间：日本时间 08:20–08:45 左右（美东 9 月 20 日晚，休市）。仅模拟盘；未提交测试订单。以下结果不能证明下一交易时段必然产生信号或成交。

## 结论

- OpenD 早间认证失败导致策略未启动。用户解锁 Windows 后，既有本机认证恢复程序于 08:31 恢复成功，行情、交易登录和美国期权模拟账户均验证通过；策略恢复运行。
- Discord Gateway 恢复，实时心跳 ACK 正常。当前 SPX 主策略的 Flow 是可选确认信息，Discord 断连不会单独禁止主策略开平仓。
- **上游入场数据问题尚未解决：SPX 当日期权链返回 HTTP 422 / RESPONSE_TOO_LARGE。** 主策略无法据此得到可靠的 Call/Put 方向参考，因此仍可能整晚没有开仓；不能把本地服务正常等同于交易条件具备。
- 三条实验线仍为 `control_sl15_tp_off`、`sl10_tp_off`、`sl15_tp30`。共同入场、分别核算退出；本次没有改变仓位、止损、止盈或信号定义。

## 最新官方消息与实际接口

1. [9 月 20 日官方更新公告](https://discord.com/channels/1434960637561409689/1489519795954122883/1551165492495126579)：新增 `options.chain_greeks_history`、`market.options_volume_rank`，期权链增加逐合约 `quoted_at`，Heatmap / Split View 优化收盘后到期列及颜色显示。公告没有明确宣称 SPX 旧报价和不完整链问题已修复。
2. [公开变更记录](https://docs.yehangshe.com/api/changelog)尚落后于上述公告；[期权接口文档](https://docs.yehangshe.com/api/domains/options)和实际 `/v1/openapi.json`、鉴权 discovery 一并核查。新的历史 Greeks 和成交量排行已可发现；未发现公开 VEX 能力。
3. 本机实际 REST 客户端和已连接的 Nightwatch MCP 均复现：`GET /v1/options/chain-snapshot/SPX?expiration=2026-09-21` 返回 `422 RESPONSE_TOO_LARGE`。SPXW 别名先返回 202 materializing，后同样返回 422。公开契约未提供该接口的分页或减小返回范围参数。
4. SPY 链可返回 310 个合约，但 quote / Greeks 水位为 9 月 18 日收盘附近。这在周末不能证明盘中新鲜度已恢复。
5. 新历史 Greeks 接口实测 SPX 9 月 18 日 Call：`total_contracts=469`、`returned_contracts=46`、`returned_rows=322`，同时 `_meta.truncated=false`。因此聚合标志不能证明全链完整；该结果不能替代实时全链方向排名。历史时间字符串还需依文档按来源时区解释。
6. [原数据故障讨论](https://discord.com/channels/1434960637561409689/1550337663276679218/1550337663276679218)当前可见内容没有维护方修复确认。[维护方 9 月 20 日回复](https://discord.com/channels/1434960637561409689/1534900873107341413/1551239797056741520)说明 OI 更新时间没有保证；不能把 OI 日期当成盘中到达时刻。

## JUNKMAN 讨论及策略改进判断

- [junk_man 9 月 20 日讨论](https://discord.com/channels/1434960637561409689/1534900873107341413/1551090246370721823)：盘前 OI 用于结构参考，通常不必高频刷新 Heatmap；SPX 更适合其指数观察；需要回测明确入场信号。
- [最近 SPXW 分析](https://discord.com/channels/1434960637561409689/1515786763417813094/1550498944402984983)：节点需要独立收复 / 回踩确认，负 Gamma 节点不是必然支撑，目标已到达后不追单，比较剩余目标空间与原始止损距离。
- 当前代码已有收盘五分钟 K 线、节点反应、回踩确认、跳过已达到目标和影线失效位。本次优先修复可验证的可靠性缺陷；尚无回测证据支持放松门槛或扩大到个股。新增排行和历史接口仅供研究客户端使用。
- 覆盖范围：读取公告、API 讨论、原故障线程、JUNKMAN 分析及 Heatmap 频道的近期可见消息，不代表服务器所有历史聊天已完整读取。未向 Discord 发送消息。

## 本次代码

- 共用方向链来源校验：对比声明合约总数与实际条数；验证新增 quote / underlying 水位、逐合约报价时效及重复身份。拒绝截断或旧的竞争合约参与最高 GEX 排名。
- 对 400 / 404 / 422 使用至少五分钟重试间隔，并尊重更长的上游重试时间；正常退出检查周期继续保持。
- 方向链响应完成后用当前时间重评数据；下单执行器不再沿用轮询开始时间。账户检查之后、提交之前重验计划、信号、GEX、报价有效期与开仓时间窗口，超时即不提交。
- 控制台将运行、券商认证、入场数据及交易信号分别展示；旧认证快照不再显示为当前已连接。增加上游错误和最近盘前检查结果。
- 新增只读 `npm run junk:preflight`（可指定 `-- --date=2026-09-21`），检查下一交易日链、GEX、API 能力/配额、模拟账户、持仓和订单。报告写入忽略的 `logs/junk-preflight.json`；关键依赖失败返回非零状态。券商读取和网络请求均有超时。

## 验证与今晚条件

- 全套 `npm test`：382 项通过、0 失败，包括数据截断、逐合约旧报价、错误退避、状态快照失效，以及账户查询延迟后过期报价 / 越过开仓截止时间不得调用下单接口的回归。
- 最新只读券商探测：`qotLogined=true`、`trdLogined=true`、`trdEnv=0`、`simAccType=4`，持仓为空、订单数为 0。未修改模拟盘保护配置。
- 最终代码通过既有守护进程重启加载，并再次检查 `/api/status`；运行恢复不代表上游链修复。
- 正常开仓仍需要当日完整、新鲜、身份正确的期权链与 GEX，五分钟价格确认、合格期权报价和全部执行门槛通过。维护方需解决 SPX 422 / 完整链可用性；开市后的实时新鲜度仍需实际验证。
- 已有仓位的退出依赖模拟券商持仓、报价与退出规则；不等待 Discord 或 Nightwatch 给出新的开仓信号。本次零持仓，未进行真实订单成交验证。
