# 2026-09-11 JUNK 主线大幅亏损复盘

核验时间：2026-09-11 22:18 UTC。以下交易时间均为美东夏令时。

后续状态（2026-09-12 本机时间）：下述退出缺陷已修复并部署到 JUNK 主线；9/11 按用户要求标记为作废交易日。原因和原始成交证据保留，调整后策略收益不再计入该日。详见 `config/trade-day-validity.json` 和 `tests/junk-pending-exit.test.mjs`。

## 结论

可明确定位的主要问题是**退出订单管理缺陷**，不是策略要求持仓跌到接近归零。入场通过了当时的既定条件；两条 TP20 子线发出未成交的止盈限价单后，整组持仓的后续止损、保本和结构失效判断被提前返回分支跳过，直到 15:55 强平才取消旧单。

这是代码缺陷定位，不是对假设成交结果的保证。缺少逐次历史可成交报价，不能断言正常退出一定盈利，或能恰好按 -10%/-15% 成交。

## 实际时间线

合约：`SPXW260911P7655000`。计划：`zero_dte_e627b30a2d2ebe8fafa5`。

| 美东时间 | 证据和行为 |
| --- | --- |
| 10:45:50 | 提交 8 张买单，限价 13.10。入场计划 gate.passed=true，bear / breakout_retest。 |
| 10:46:01 | 记录完整成交，均价 12.30；七条标准子线和一条观察子线各分配 1 张。 |
| 10:47:49 | sl10_tp20、sl15_tp20 触发 TP20，合并挂出 2 张、14.80 的限价卖单。触发止盈不等于卖出成交。 |
| 11:05:49 | 决策记录价格 7661.5357，已越过该空头计划的结构失效价 7661.4781；但挂单分支绕过了重新计算退出计划的循环。 |
| 15:55:19 | 日志确认此前的 2 张限价单已取消，累计成交为 0；整组仍剩 8 张。 |
| 15:55:35 | 提交 8 张市价强平单。 |
| 15:55:50 | 记录全部成交，均价 0.45，各子线平仓。 |

独立 OpenD 只读查询再次确认：买入 8 张均价 12.30；14.80 的 2 张卖单状态 15（取消）、成交 0；最终卖出 8 张均价 0.45。目前模拟账户零持仓、零活动订单。

每条标准子线当天均为：投入 $1,230，卖出 $45，毛亏损 **-$1,185，投入收益率 -96.34%**。观察子线结果相同。这里只逐线描述，不将实验子线相加当作策略收益；未扣费用，严格说不是 100% 全损。

## 代码原因与来源

- `apps/zero-dte-options/zero-dte-line.mjs` 的 `reconcile_junk_experiment_row` 先处理已有退出订单（约 2382 行）。
- 未成交限价单仅在 `force_close` 时请求撤单（2529 行）；随后在 2543 行直接返回。
- 对全部子线重新计算退出触发条件的循环在约 2589 行，位于该返回之后，因此两条子线的未成交订单阻塞了整组其他子线。
- `apps/zero-dte-options/zero-dte-moomoo-exit.mjs` 约 397 行规定：空头标的价格达到或超过失效价时触发结构退出。因此这里是已有退出规则未被继续执行，不是需要额外增加策略门槛。
- Git blame 将该提前返回及仅强平撤单分支追溯至 `5c85381a`（2026-08-12，Consolidate repository around JUNKMAN simulation）。这是仓库可见来源，不代表能够确定更早的开发意图。
- 最终 TP20 子线仍带有 `option_20pct_take_profit` 标签，是先前触发原因的持久化记录；实际成交价格是 0.45，不能把该标签当作止盈成功。

Moomoo 官方的 [ModifyOrder 文档](https://openapi.moomoo.com/moomoo-api-doc/en/trade/modify-order.html) 提供显式改单、撤单操作。没有证据表明本次是 OpenD 自动止损失灵；这里发出的是普通限价卖单，程序需要持续管理未成交部分与其他持仓的退出。

## 指标和延迟的证据边界

- 入场时 GEX 快照年龄约 347 秒，未超过当前允许的 600 秒；价格确认和入场 gate 均通过。不能仅凭之后亏损认定 Nightwatch 指标错误，也不能把通过入场条件视作盈利保证。
- 挂单至强平之间仍有 872 条决策记录，并非五小时程序完全断线。
- 确有六处决策记录间隔约 646–647 秒，起点分别为 11:19:50、11:44:51、13:44:58、14:39:46、14:59:47、15:29:48。仅凭决策间隔不能断定是网络断线，也不能证明该期间每次经纪商对账正常。
- 11:05 已出现结构失效价格，早于第一处长间隔；即使网络持续正常，已有挂单分支也会继续绕过退出计算。因此这些间隔不是解释整组一直持有到 15:55 所必需的原因。网络/调度是否有额外影响尚未完全归因。

## 证据文件

- `logs/zero-dte-options-entry-plans.ndjson` 第 467 行：入场计划。
- `logs/zero-dte-options-trades.ndjson` 第 2642–2653 行：买入、分配和未成交 TP20 挂单；第 2654 行以后：强平及逐线成交。
- `logs/zero-dte-options-decisions.ndjson`：价格与决策连续性。
- `logs/zero-dte-options-experiment-events.ndjson`：撤单后重试及逐线退出事件。
- `logs/zero-dte-options-runtime-state.json`：最终持仓账本。

## 本次变更及未完成项

按要求将启用配置、top stack、npm 命令和实时收益页面收敛为 JUNK 主线；MULTI/FLOW 历史实现与数据保留，直接运行入口拒绝启动，旧 PowerShell 入口变为退出提示。保留主线内部各止盈止损实验子线，不擅自替用户选择其中一条。

初次诊断时尚未修改退出算法。随后用户授权修复：现已在活动退出单存在时持续评估全部剩余子线，在价格移离限价或其他子线/高优先级退出变化时撤单，等待经纪商终态并核对累计成交后才提交剩余数量。已增加 9/11 路径、部分成交、撤单/成交竞态、撤单失败、重复回报和只读模式回归测试；未修改原有策略阈值。

初次运行切换被执行环境拦截。后续修复部署时已重新核验经纪商和本地零持仓、零活动订单，成功退出旧进程并隐藏启动修复后的 top stack；MULTI/FLOW 已停止，OpenD 未被重启。
