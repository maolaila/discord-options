# JUNK 主线退出管理修复及 9/11 作废记录

## 已实施

- 修复 `reconcile_junk_experiment_row`：存在未成交退出单时仍持续更新全部剩余子线的峰值、保本状态和退出计划。
- 沿用已有退出规则优先级，止损/保本/结构失效可覆盖旧止盈意图；实际闭仓不会再只沿用已过时的 TP20 标签。
- 剩余退出分配或优先级变化、当前卖出限价低于旧挂单价、到达原有强平时点时，撤销旧限价单。不是新增策略过滤门槛或固定时间止损。
- 撤单 ACK/超时不视为订单已终止。保持旧订单身份，失败可重试；确认经纪商终态并计入累计成交后，下一轮刷新可卖数量，再提交剩余仓位。
- 原始成交日志和账本保留；`config/trade-day-validity.json` 将整个 2026-09-11 美东交易日标记为 `void_for_strategy_performance`。调整后收益排除当日盈亏、成本、卖出收入、交易/胜负统计、Profit Factor 和资金占用；持仓和原始风控核对不受该报表规则影响。
- 实时收益页面、实验收益视图、交易记录标记和复盘导出均包含作废信息，页面明确区分调整后策略收益与完整账户收益。历史链上证明未改写。

## 验证

- 全量 Node 测试 355 项通过，0 失败。
- 其中 11 项退出管理回归覆盖：9/11 卡单路径、单纯价格移离挂单、撤单期间部分成交、撤单与全成交竞态、撤单失败重试、dry-run、不带报价的原有强平、累计回报倒退、撤单过程中重启恢复、剩余仓位最终完整成交、活动市价单不重复提交。
- 4 项作废报表测试覆盖日期边界、所有指标/资金利用率、整组跨日记录、原始风险和数据不变；其余原有测试继续通过。
- 9/11 实际匹配 1 组入场、8 条逐线闭仓、31 条关联事件。各子线的调整后累计盈亏比原始增加 $1,185、成本减少 $1,230、成交笔数减少 1；只是剔除整笔，不改造其真实成交结果。
- `/api/junk-performance` 返回 schema 2、`latest_date_et=2026-09-10`、8 条作废闭仓及原始盈亏审计明细。

## 退出修复版部署（先前完成）

部署前独立 OpenD 只读查询验证 qot/trd 登录正常、trdEnv=0、模拟期权账户 simAccType=4，且经纪商与本地均零持仓零活动单。

已停止经核验的旧应用进程，隐藏启动修复版。top supervisor PID 70428、JUNK supervisor PID 7324、JUNK watcher PID 51500、console PID 57812；MULTI/FLOW 不再运行。OpenD 和浏览器未重启。33333/9222/18766 正常监听，Gateway ACK、主线心跳及报表新口径均已读回验证。

当时市场已收盘，主线为 `idle_market_closed` / `execute_simulate` / `real_trading_allowed=false`。没有伪造信号或下测试单。

这是针对该代码缺陷的修复与回归验证，不是对交易盈利、止损精确成交价、网络或经纪商可用性的绝对保证。

## 三线精简（本次提交）

- 用户确认仅保留 `control_sl15_tp_off`、`sl10_tp_off`、`sl15_tp30`；生产配置版本升级为 4。其余退出实验线、regime/lifecycle 观察线不参与新入场，MULTI/FLOW 等其他业务线保持停用。
- 三线共用原有 JUNKMAN 入场及结构退出规则，没有新增策略门槛。未固定止盈不等于不退出：原有保本、结构失效、节点目标及收盘退出仍适用。
- 新入场按三条独立线等量分配；旧账本冻结定义不变。三条保留线的既有有效历史继续统计，9/11 作废规则不变。
- 收益页面只展示三条线，并清除旧七线/观察线页面缓存；记录未删除。
- 全量 `npm test` **361 项通过，0 失败**。保留八线历史样本验证原始故障，另增三线 TP30 挂单、撤单成交竞态、等量分配、旧仓配置切换及报表测试。

### 三线运行部署尚待完成

2026-09-11 22:59 UTC 独立 OpenD 查询：qot/trd 登录正常，trdEnv=0、simAccType=4；经纪商和本地均零持仓、零活动订单，无旧账本配置冲突。

随后隐藏重启命令被执行权限策略拒绝，命令未执行，未绕过限制。23:00 UTC 只读复核显示原 watcher PID 51500 仍为配置版本 3（八条线），心跳正常、模拟盘模式、市场已收盘。OpenD PID 73336、浏览器 PID 49392、控制台 PID 57812 未改变，Discord Gateway ACK 正常。收益 API 因每次读取配置文件已展示三条，但这不代表旧 watcher 已热更新。

需要在本机完成一次受控的主程序重新加载，确认运行状态中 `experiment.manifest.version=4` 且仅有上述三条线后，才能宣称三线停用变更已部署。重新加载前必须再次核对无持仓/活动订单；不得将修改状态文件或报表当作运行生效证明。
